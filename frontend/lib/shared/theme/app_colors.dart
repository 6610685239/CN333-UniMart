import 'package:flutter/material.dart';

class AppColors {
  static const Color ink        = Color(0xFF141414); // primary text, buttons, dark surfaces
  static const Color textMuted  = Color(0xFF737373); // secondary text
  static const Color textHint   = Color(0xFFA3A3A3); // hints, inactive — never for real info
  static const Color bg         = Color(0xFFFAFAF7); // screen background
  static const Color surface    = Color(0xFFFFFFFF); // cards, inputs
  static const Color border     = Color(0xFFD8D6CF); // outlines
  static const Color divider    = Color(0xFFE8E6DF);
  static const Color accent     = Color(0xFFF5C518); // TU yellow — ONE accent per screen
  static const Color accentSoft = Color(0xFFFEF2C2); // soft yellow fill
  static const Color success    = Color(0xFF3EB76B); // online dot, confirmations
}
